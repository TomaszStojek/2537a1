<%- include("templates/header") %>
<div class="container">
  <div class="mt-4">Users:</div>
  <ul class="list-group mt-2">
    <% let i = 0; %>
    <% for (i = 0; i < users.length; i++) { %>
      <%- include('templates/user', { user: users[i] }) %>
      <div class="my-2"></div> <!-- Add a margin-bottom to create space between user templates -->
    <% } %>
  </ul>
</div>
<%- include("templates/footer") %>
